import { defineStore } from 'pinia';

/**
 * █ [STORE] :: PROJECTS STORE
 * =====================================================================
 * DESC:   Estado global para proyectos.
 * STATUS: WIP [TODO: Implementar lógica]
 * =====================================================================
 */
export const useProjectsStore = defineStore('projects', () => {
  return {};
});
