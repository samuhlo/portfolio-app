import { defineStore } from 'pinia';

/**
 * █ [STORE] :: APP STORE
 * =====================================================================
 * DESC:   Estado global de la aplicación (UI, config general, etc).
 * STATUS: WIP [TODO: Implementar lógica]
 * =====================================================================
 */
export const useAppStore = defineStore('app', () => {
  return {};
});
