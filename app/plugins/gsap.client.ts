/**
 * █ [PLUGIN] :: GSAP CLIENT
 * =====================================================================
 * DESC:   Plugin para registrar/inyectar configuración global de GSAP.
 * STATUS: WIP [TODO: Añadir registro de plugins de GSAP si es necesario]
 * =====================================================================
 */
export default defineNuxtPlugin((nuxtApp) => {
  // empty
});
