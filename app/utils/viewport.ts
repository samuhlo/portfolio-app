/**
 * █ [COMPOSABLE] :: VIEWPORT
 * =====================================================================
 * DESC:   Cálculo y obtención de dimensiones del viewport.
 * STATUS: STABLE
 * =====================================================================
 */

/**
 * ◼️ GET VIEWPORT DIMENSIONS
 * ---------------------------------------------------------
 * Retorna ancho y alto de la ventana visible.
 * [TODO]: Implementar lógica de window.innerWidth/innerHeight.
 */
export const getViewportDimensions = () => {
  return { width: 0, height: 0 };
};
