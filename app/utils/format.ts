/**
 * █ [COMPOSABLE] :: FORMAT
 * =====================================================================
 * DESC:   Utilidades de formateo general (fechas, strings, etc.)
 * STATUS: STABLE
 * =====================================================================
 */

/**
 * ◼️ FORMAT DATE
 * ---------------------------------------------------------
 * Formatea una fecha a string.
 * [TODO]: Implementar configuración regional (locale).
 */
export const formatDate = (date: Date) => {
  return '';
};
